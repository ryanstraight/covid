# Add any project specific configuration here.
add.config(
  apply.override = FALSE,
  plot_footnote = "Straight - MIS 545"
)

# Add project specific configuration that can be overridden from load.project()
add.config(
  apply.override = TRUE
)
